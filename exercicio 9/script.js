const base = parseInt(prompt("Digite a base do triangulo:"));
const altura = parseInt(prompt("Digite a altura do triangulo:"));



const area = (base*altura)/2;

console.log(`A area do triangulo = ${area}.`);


alert(`A area do triangulo = ${area}.`);